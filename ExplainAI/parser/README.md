# Parser Notes

ExplainAI currently uses Python's built-in `ast` module for Python-aware analysis and lightweight heuristics for other supported languages.

Future upgrades can replace the heuristics with Tree-sitter grammars for JavaScript, Java, and C++.
